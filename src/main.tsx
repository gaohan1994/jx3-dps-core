import React from 'react';
import ReactDOM from 'react-dom';
import JDCDemoApp from './demo/demo';

ReactDOM.render(
  <React.StrictMode>
    <JDCDemoApp />
  </React.StrictMode>,
  document.getElementById('root')
);
